# 领域约定

入境服务凭证争议台围绕多语言服务异议、代理关系和调解决定保存业务事实。主要对象为游客别名、凭证摘要、案件、代理授权、材料披露、调解决定、金额账项、期限。

必须长期遵守以下约束：疑似重复只能人工合并；授权撤回阻止代理查看新材料；金额更正追加记录；期限按服务发生地时区计算。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。